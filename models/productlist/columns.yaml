# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: Name
        searchable: true
    slug:
        label: Slug
    description:
        label: Description
    products_count:
        label: Products Count
        relation: products_count
        valueFrom: count
        default: 0