# ===================================
#  Form Field Definitions
# ===================================

fields:
    is_update:
        label: Update existing data
        type: checkbox
        default: true