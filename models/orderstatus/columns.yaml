# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: Name

    is_active:
        label: Active
        type: switch

    send_email:
        label: Email customer
        type: switch

    send_email_to_admin:
        label: Email admin
        type: switch

    attach_pdf:
        label: Attach invoice pdf
        type: switch

    mail_template:
        label: Mail template
        relation: mail_template
        select: code

    admin_mail_template:
        label: Mail template (Admin)
        relation: admin_mail_template
        select: code
