# ===================================
#  Form Field Definitions
# ===================================

fields:
    status:
        label: New Status
        type: relation
        emptyOption: - select a status -

    note:
        label: Note
        type: textarea
        size: small
