# ===================================
#  Form Field Definitions
# ===================================

fields:
    status:
        label: New Status
        type: dropdown
        placeholder: -- select status --

    note:
        label: Note
        type: textarea
        size: small
