# ===================================
#  List Column Definitions
# ===================================

columns:
    timestamp:
        label: Timestamp
        type: datetime
    status:
        label: Status
        relation: status
        select: name
        sortable: false
    note:
        label: Note
        searchable: true